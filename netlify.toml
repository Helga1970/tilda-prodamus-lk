[build]
  command = "npm install"        # Устанавливаем все зависимости из корня
  functions = "netlify/functions"
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
